SELECT com. id AS company_id
INTO company_id
FROM device dev
INNER JOIN contract con ON dev. contract_id - con.id
INNER JOIN user us ON con. user_id = usr. id
INNER JOIN company com ON con. company_id - com.id
WHERE dev. serial = deviceSerial COLLATE utE8mb4_0900_ai_ci
AND dev. linked_yn 'Y':